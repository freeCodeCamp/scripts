{
  "name": "scripts",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "install": "echo 'Packages should be installed at the script level, not the root level.'"
  }
}
